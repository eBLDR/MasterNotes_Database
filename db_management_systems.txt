SQL
SQLite3
It is not a client-server engine, rather it is
self-contained, embedded into the end program,
storing the data in a single disk file.
It is fast and lightweight.

MySQL
It is client-server based, running on a server.
Default server port: 3306

NO-SQL

MongoDB
It works on a server.
A cross-platform document-oriented database program.
It uses JSON-like document with schema.
Default server port: 27017
